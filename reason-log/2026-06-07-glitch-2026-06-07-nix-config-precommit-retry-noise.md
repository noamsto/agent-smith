# glitch-2026-06-07-nix-config-precommit-retry-noise

**Artifact:** /home/noams/nix-config/CLAUDE.md  
**Fix type:** escalate-out-of-instructions  **Confidence:** low  **Date:** 2026-06-07

## Diagnosis

This is a low-confidence, heterogeneous 'retry' cluster with no single behavior that CLAUDE.md prose can fix. Three of the four incidents are unrelated routine edits (a HANDOFF.md fix in a DIFFERENT repo, a TODO.md edit + case-rename cleanup, a hyprland binding rename) where the only 'retry' is normal Read-then-Edit-then-stage flow — not a glitch. The one mechanically reproducible signal is incident 3 (42f3ae7a:18): `git commit` triggered the pre-commit framework's 'Unstaged files detected → stashing' path, which forces an apparent commit retry whenever the working tree has unstaged changes alongside staged ones. No prose rule in CLAUDE.md addresses this, and prose cannot reliably prevent it because the stash/unstash is driven by pre-commit's own staging model, not by agent behavior.

## Evidence

- 0d2582bb:244
- 0725da93:76
- 42f3ae7a:18
- 43aa7a0f:284
- 4 sessions

## Proposed change

```
Do NOT add prose to CLAUDE.md (the file is already large and the signal is mostly noise). Instead make the only real failure impossible at the tooling layer:

Hook: a PreToolUse hook on Bash matching `git commit *`, installed in the nix-config repo's local Claude settings (the `--settings` overlay generated by `home/ai/claude-code/default.nix`, since it can reference a /nix/store script; the JSON `hooks` key lives there per the global settings-architecture rule).

Sketch (`pre-commit-autostage` guard):
  - Trigger: PreToolUse, tool=Bash, command matches /\bgit commit\b/.
  - Action: if `git diff --name-only` (unstaged tracked changes) is non-empty while there are also staged changes, emit a systemMessage: "Unstaged tracked changes present — pre-commit will stash them and the commit may appear to retry. Run `git add -u` first or commit with `-a` if intended." Optionally non-blocking (exit 0 with advisory) so it informs rather than aborts.

This turns the surprising pre-commit stash/retry into an explicit, expected step and eliminates the only reproducible retry in the cluster without duplicating guidance or bloating CLAUDE.md.
```

## Expected effect

Drove by the 'retry' signal, but all four incidents carry confidence:low and do not share one behavior — incident 1 is in a different repository (agent-smith), incidents 2 and 4 are ordinary edit-then-stage sequences with no error, and only incident 3 shows a real, mechanical retry (pre-commit unstaged-file stashing). CLAUDE.md already covers git/pre-commit gotchas via the `nix-rebuild` skill reference, so `add`/`strengthen` would duplicate or pad with no leverage over a framework-driven behavior. Escalating the single reproducible case to a PreToolUse Bash hook is the only change that could actually remove the retry; expected effect is informing the agent before commit so the stash step is anticipated, not retried-into. Confidence low because the cluster is dominated by non-glitch noise and the actionable signal appears in just one session.

**PR:** https://github.com/noamsto/nix-config/pull/5

<!-- outcome appended by deja-vu -->
